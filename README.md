A low-feature (Arabic) version of WhatsApp using Reactjs, Nodjs and MongoDB

To run 

"npm install"

then "npm start"

in both server and web folders


don't forget to install MongoDB and connect the database
